'use strict';

describe('homePage module', function() {

  beforeEach(module('homePage'));

  describe('homePage controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var crl = $controller('homePageController');
      expect(crl).toBeDefined();
    }));

  });
});